import React from "react";
import "./App.css";
import { styled } from "styled-components";

export function App() {
  return <AppWrapper>hii</AppWrapper>;
}

const AppWrapper = styled.div`
  height: 100vh;
  overflow: hidden;
  position: relative;
`;
